Here i Write a code to merge and sort the array without using the extra space

import java.util.Arrays;
class Solution{
  public void merge(int[] nums1, int m, int[]num2,int n){
    for(int i=0;i<n;i++){
      nums1[m+1]==num2[i];
    }
    Arrays.sort(nums1);
  }
